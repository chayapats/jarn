# docs/assets

Static assets embedded in documentation.

## Hermes display-parity (P5)

- [UAT-HERMES-PARITY.md](UAT-HERMES-PARITY.md) — CLI + Telegram checklists.
  Live terminal/bot rows are **Not run**. Pytest-covered rows name the test
  file on the stacked code PRs. No row is Pass.
- Text mockups:
  - [cli-paste-preview.md](cli-paste-preview.md)
  - [cli-sessions-picker.md](cli-sessions-picker.md)
  - [telegram-verbose-bubble.md](telegram-verbose-bubble.md)
  - [telegram-busy-ack.md](telegram-busy-ack.md)

## Optional `demo.gif`

**Status (v0.10.0): no demo GIF is tracked.** The main READMEs deliberately do not
embed the missing path, so GitHub/npm previews stay free of broken images.

**P5-3 (2026-08-15):** `vhs` was **not installed** in the evidence environment,
so `scripts/record-demo.sh` was not run and **no GIF was recorded**. Missing
`vhs` must not fail the close-out. Re-record later on a machine with
[vhs](https://github.com/charmbracelet/vhs) and `gifsicle`.

The demo GIF is generated by running:

```bash
./scripts/record-demo.sh
```

This requires [vhs](https://github.com/charmbracelet/vhs) (`brew install vhs`) and
`gifsicle` (`brew install gifsicle`).  The script runs `demo.tape`, which sets
`JARN_DEMO=1` so the recording is fully deterministic (canned-response provider —
no real API key needed).  Target size: < 3 MB.

After recording, verify the file is below 3 MB, commit it, and then add a Markdown
image named `jarn demo` pointing to `docs/assets/demo.gif` in both `README.md` and
`README-TH.md`.
